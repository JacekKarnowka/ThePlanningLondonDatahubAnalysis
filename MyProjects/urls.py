from django.urls import re_path
from django.urls import include
from . import views
from MyProjects.views import created_projects

# from MainApp.views import Table

urlpatterns = [
    # re_path(r'^add_app/$', add_app.as_view(), name ="add_app"),
]
